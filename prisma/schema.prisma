generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model PF_usuario {
  id              Int       @id @default(autoincrement())
  nomeCompleto    String
  telefone        String
  email           String    @unique
  senha           String
  dataCadastro    DateTime  @default(now())

  papelUsuarioID  Int
  papelUsuario    PF_papelUsuario @relation(fields: [papelUsuarioID], references: [id])

  postagens       PF_postagem[]
}

model PF_papelUsuario {
  id            Int          @id @default(autoincrement())
  papelUsuario  String

  usuarios      PF_usuario[]
}

model PF_postagem {
  id             Int       @id @default(autoincrement())
  titulo         String
  descricao      String
  visibilidade   Boolean
  dataPublicacao DateTime  @default(now())
  caminhoImagem  String

  autorID        Int
  autor          PF_usuario @relation(fields: [autorID], references: [id])
}